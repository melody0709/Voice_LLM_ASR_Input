# 2026-08-12

## VoxType Settings UI hint truncation fix
- Fixed truncated hint text in Cloud ASR → Qwen ASR settings tab by adding `SS_LEFT` to `CreateHint()` so static labels wrap.
- Added `QwenHint2LineH` constant and expanded vertical spacing for Language, Chunk ms, Language hints, and Advanced hint rows.
- Adjusted Qwen Advanced dialog layout constants and shortened multi-line hint text so all hints fit within their 2-line height.
- Build succeeded; `validate_settings_layout.ps1` passed at 96/144/192/288 DPI.
- Modified: `src/app/globals.h`, `src/ui/settings.cpp`.

## Round 2: hint line-height fix (user reported vertical clipping after round 1)
- Root cause: `QwenHint2LineH` was 44 but single-line hint is 24 → two lines need 48; second line bottom edge was clipped.
- Bumped `QwenHint2LineH` to 48 and shifted subsequent Qwen rows +4/+8/+12 (main window) and Advanced dialog Y constants accordingly; window/dialog sizes unchanged.
- Fixed `SS_LEFT` regression: Advanced dialog "Range: -1.0 to 1.0" (width 135) wrapped and clipped → shortened to "-1.0 to 1.0".
- Language hint text shortened to avoid orphan second-line word.
- Build passed; layout validation OK at 96/144/192/288 DPI.

## Round 3: hint copy simplification (user found texts verbose)
- Rewrote all Qwen Settings hints shorter (approved table in conversation): e.g. Language → "Fallback when Language hints is blank.", Advanced → "Hotwords, semantic punctuation, VAD thresholds, heartbeat."
- Renamed checkbox "Input field context" → "Use input field text as context" (label was jargon, user said it was unclear).
- Chunk ms hint has THREE dynamic variants in ApplyQwenModelProfile() (http/streaming/legacy) + initial creation text — all four must stay in sync when editing.
- Kept 2-line hint heights (48) unchanged per plan A; no coordinate changes this round.
- Build passed.
# 2026-08-12

- 完成 Qwen 三模型第二轮官方文档符合性审查（报告：.plan/fix/QWEN_DOC_COMPLIANCE_REVIEW_2026-08-12.md）。
- 结论：三路协议与官方文档匹配；发现 2 个健壮性 bug——Qwen3 realtime 与 Audio 3 streaming 在已拿到 final 文本但缺 session.finished/task-finished 时，replay 失败会丢弃已识别文本（qwen_streaming_session.cpp 最终等待循环先判 drainFailed 后判 drainCompleted）。
- 低优先级：HTTP sample_rate 官方示例是字符串 "16000" 代码发数字；session.update 空 input_audio_transcription 建议省略；disfluency_removal_enabled/special_word_filter/continue-task/连接复用未接入。

- 修复 Qwen 两个健壮性 bug 并编译+离线测试通过：
  - Bug 1 (qwen_streaming_session.cpp): 最终等待循环改为先判 drainCompleted/drainSessionFinished 再判 drainFailed，已拿 final 文本后 peer-close/缺 session.finished 不再降级失败；新增 QwenRealtimeSessionDebugLog 与 transcript_completed_without_session_finished 日志。
  - Bug 2 (qwen_audio_streaming_session.cpp): 新增 finalizePhaseError 标记，finish-task 发送后的 task-finished 缺失/peer-close 且已有 committed 文本时恢复采用，不清空；中流失败仍走 replay。
  - ./build.bat 通过；./build.bat --test: qwen_free_protocol_test/qwen_audio_json_test 均 PASS。
